<Page
    x:Class="LeaderboardUno.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LeaderboardUno"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:LeaderboardUno.Shared.Models"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <muxc:NavigationView Name="NavView" PaneTitle="Games" PaneDisplayMode="Auto" ItemInvoked="NavView_ItemInvoked" IsSettingsVisible="False" MenuItemsSource="{x:Bind games}">
            <muxc:NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="data:Game">
                    <NavigationViewItem Tag="{x:Bind id}" Content="{x:Bind GameName}" Icon="Play"/>
                </DataTemplate>
            </muxc:NavigationView.MenuItemTemplate>
            <Frame Name="ContentFrame" Margin="0,20,0,0"/>
        </muxc:NavigationView>
        <Button Click="Button_Click" FontFamily="Segoe MDL2 Assets" Content="&#xE72C;" VerticalAlignment="Bottom" Margin="0,0,0,7" Background="Transparent"/>
    </Grid>
</Page>
